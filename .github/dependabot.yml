version: 2
updates:
  # GitHub Actions — SHA-pinned third-party actions in CI get auto-bumped here.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
